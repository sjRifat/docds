name: Automated Docds workflow
on:
  push:
jobs:
  start-job:
    runs-on: ubuntu-latest
    steps:
    - name: Checkout Code
      uses: actions/checkout@v4

    - name: Log in to Docker Hub
        uses: docker/login-action@v3
        with:
          username: safayatjakir
          password: dckr_pat_0sxyqPnM9MZxGcQPjCa6NJRdA0k

    - name: Build Docker image
        run: |
          docker build -t safayatjakir/docdsv2:latest .
    - name: Push Docker image
      run: |
        docker push safayatjakir/docdsv2:latest